name: crm_track
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.4 <4.0.0"

dependencies:
  bloc: ^9.0.0
  dotted_line: ^3.2.3
  flutter:
    sdk: flutter
  flutter_bloc: ^9.0.0
  flutter_modular: ^6.3.4
  intl: ^0.20.2
  meta: ^1.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/images/
    - assets/icon/
